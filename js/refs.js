const refs = {
  linkListEl: document.querySelector(".list-group"),
  containerEl: document.querySelector(".container"),
  postsListEl: document.querySelector(".posts-container"),
  commentsListEl: document.querySelector(".comments-list"),
  postContainer: document.querySelector(".post-container"),
};
const {
  linkListEl,
  containerEl,
  postsListEl,
  commentsListEl,
  postContainer,
} = refs;
